void setup() {
  pinMode(2,OUTPUT); //2. pin e çıkış verdik.
}

void loop() {
  digitalWrite(2,HIGH); //2. pin e 5V verildi ve yandı.
  delay(200);           //200ms bekleme.
  digitalWrite(2,LOW);  //2. pin e 0V verildi ve söndü.
  delay(200);           //200ms bekleme.
}
